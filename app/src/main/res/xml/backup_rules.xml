<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The API key lives in this DataStore file and is encrypted with an Android Keystore
         key, which never leaves the device. Never back it up. -->
    <exclude domain="file" path="datastore/manzil_secrets.preferences_pb" />
</full-backup-content>
